// next.config.mjs
const nextConfig = {
  reactStrictMode: true,
  output: "standalone", // Jeśli używasz aplikacji jako PWA, upewnij się, że to ustawienie jest prawidłowe.
};

export default nextConfig;
